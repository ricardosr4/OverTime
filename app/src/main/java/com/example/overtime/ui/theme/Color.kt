package com.example.overtime.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Colores Base
val PrimaryColor = Color(0xFF4A90E2) // Botón principal, enlaces interactivos
val SecondaryColor = Color(0xFFA0EE9A) // Elementos secundarios, acentos
val CardColor = Color(0xFFBFE6FD) // Color de card principal
//val BackgroundColor = Color(0xFFF5F7FA) // Fondo principal de la app
//val ContrastColor = Color(0xFFFF6B6B) // Botón de acción destacada, alertas importantes

// Botones
val ButtonPrimary = Color(0xFF4A90E2) // Fondo del botón principal
val ButtonPrimaryText = Color(0xFFFFFFFF) // Texto del botón principal

//val ButtonSecondaryBorder = Color(0xFF4A90E2) // Borde del botón secundario
//val ButtonSecondaryText = Color(0xFF4A90E2) // Texto del botón secundario
val ButtonDisabled = Color(0xFFE0E0E0) // Fondo del botón deshabilitado
//val ButtonDisabledText = Color(0xFF9E9E9E) // Texto del botón deshabilitado

// Textos
val TextPrimary = Color(0xFF212121) // Texto principal
//val TextSecondary = Color(0xFF757575) // Texto secundario
val TextHint = Color(0xFF867F7F) // Texto de ayuda (hints)

// Errores y Validaciones
//val ErrorText = Color(0xFFD32F2F) // Mensajes de error
//val ErrorBorder = Color(0xFFFFCDD2) // Bordes de campos con error

//// Notificaciones y Estados
//val SuccessColor = Color(0xFF81C784) // Indicadores de éxito
//val WarningColor = Color(0xFFFFB74D) // Advertencias

//// Sombras y Separadores
//val ShadowColor = Color(0xFFBDBDBD) // Sombras sutiles
val DividerColor = Color(0xFFE0E0E0) // Líneas divisorias

// Placeholders
//val PlaceholderBackground = Color(0xFFEDEDED) // Fondo para placeholders

val BluePrimary = Color(0xFF1E88E5)      // Azul vibrante (botones principales)
val BlueDark = Color(0xFF1565C0)         // Azul oscuro (acciones secundarias)
val YellowSecondary = Color(0xFFFFC107)  // Amarillo dorado (botones de advertencia)
val OrangeVariant = Color(0xFFFFA000)    // Naranja dorado (acciones importantes)

val Background = Color(0xFFF5F5F5)       // Gris claro (fondo general)
val Surface = Color(0xFFFFFFFF)          // Blanco (tarjetas y contenedores)
val Error = Color(0xFFD32F2F)            // Rojo (alertas y errores)

val OnPrimary = Color(0xFFFFFFFF)        // Blanco (texto sobre azul)
val OnSecondary = Color(0xFF000000)      // Negro (texto sobre amarillo)
val OnBackground = Color(0xFF212121)     // Negro intenso (texto principal)
val OnSurface = Color(0xFF424242)        // Gris oscuro (texto en tarjetas)

val Hint = Color(0xFF9E9E9E)             // Gris medio (hints en input fields)
val Disabled = Color(0xFFBDBDBD)         // Gris claro (botones deshabilitados)
val ButtonBar = Color(0xFF263238)        // Azul grisáceo (barra de navegación)

